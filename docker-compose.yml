version: "3.6"
services:
  webapp:
    build: ./webapp
    ports:
      - 3000:3000
    volumes:
      - type: bind
        source: ./webapp
        target: /app
    environment:
      - ENV=dev
      - STATIC_ROOT=../static
      - VIEW_ROOT=../views
    command: ["go", "run", "."]